<resources>

  <!-- Base application theme. -->
  <style name="AppTheme"
      parent="Theme.AppCompat.Light.DarkActionBar">
    <!-- Customize your theme here. -->
  </style>

  <style name="MyPopup" parent="Widget.AppCompat.ListView.DropDown">
    <item name="android:popupBackground">@drawable/bg</item>
    <item name="android:divider">@drawable/divider</item>
    <item name="android:listSelector">@null</item>
  </style>

  <style name="TestTheme" parent="AppTheme">
    <item name="popupMenuStyle">@style/MyPopup</item>
    <item name="dropDownListViewStyle">@style/MyPopup</item>
  </style>

</resources>
